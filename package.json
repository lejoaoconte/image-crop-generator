{
  "name": "image-crop-generator",
  "version": "1.0.0",
  "description": "Crop your images after uploading them to the server.",
  "main": "src/components/Cropper.tsx",
  "repository": "https://github.com/lejoaoconte/image-crop-generator",
  "author": "João Pedro Conte",
  "license": "MIT",
  "outDir": "dist",
  "files": [
    "dist"
  ],
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^12.1.5",
    "@testing-library/user-event": "^13.5.0",
    "@types/jest": "^26.0.24",
    "@types/react": "^17.0.2",
    "@types/react-dom": "^19.1.3",
    "parcel-bundler": "^1.12.5",
    "typescript": "^4.1.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "clean": "rm -rf dist .cache",
    "build": "yarn clean && parcel build src/components/Cropped.tsx --out-dir dist --target node --no-source-maps",
    "publicate": "npm publish ./dist --access public"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
