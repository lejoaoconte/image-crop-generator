{
  "name": "image-crop-generator",
  "version": "1.1.9",
  "description": "Crop your images after uploading them to the server.",
  "main": "src/cropped.tsx",
  "repository": "https://github.com/lejoaoconte/image-crop-generator",
  "author": "João Pedro Conte",
  "license": "MIT",
  "files": [
    "index.js",
    "index.d.ts",
    "package.json",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "publisher": "lejoaoconte",
  "keywords": [
    "cropped",
    "Generator Image Crop",
    "Image Crop",
    "Cropper Image"
  ],
  "scripts": {
    "clean": "rm -rf dist .cache src/bundle",
    "start": "yarn clean && tsc --outDir src/bundle; parcel src/index.html",
    "build": "yarn clean && tsc",
    "publicate": "cp ./dist/cropped.js . && cp ./dist/cropped.d.ts . && mv cropped.js index.js && mv cropped.d.ts index.d.ts &&  npm publish --access public && rm -rf index.js && rm -rf index.d.ts"
  },
  "devDependencies": {
    "@types/react": "^18.0.37",
    "@types/react-dom": "^18.0.11",
    "parcel-bundler": "^1.12.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^5.0.4"
  }
}
